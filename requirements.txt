pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.2.0
nltk>=3.8.0
matplotlib>=3.6.0
seaborn>=0.12.0
wordcloud>=1.9.0
joblib>=1.2.0
jupyter>=1.0.0
ipython>=8.0.0
pytest>=7.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.25.0
pyarrow>=10.0.0

# =============================================================================
# OPCIONAL: Apache Airflow (recomendado usar via docker-compose-airflow.yml)
# Descomente abaixo apenas se deseja instalar localmente (pode gerar conflitos
# de dependencias; prefira a imagem oficial apache/airflow via compose).
# =============================================================================
# apache-airflow>=2.10.2
# apache-airflow-providers-postgres>=5.0.0
# psycopg2-binary>=2.9.0
