pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.2.0
nltk>=3.8.0
matplotlib>=3.6.0
seaborn>=0.12.0
wordcloud>=1.9.0
joblib>=1.2.0
jupyter>=1.0.0
ipython>=8.0.0
pytest>=7.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.25.0
pyarrow>=10.0.0
prometheus_client>=0.20.0

# =============================================================================
# OTIMIZACAO DE INFERENCIA (ONNX Runtime CPU + Conversao sklearn -> ONNX)
# VERSOES MINIMAS COMPATIVEIS COM PYTHON 3.13 WIN-AMD64 (tem wheels pre-compilados, evita cmake build)
# ONNX Runtime CPU (engine de inferencia otimizado, cross-platform sem dependencia GPU)
onnxruntime>=1.20.0
# skl2onnx: exporta pipelines sklearn completos (TfidfVectorizer + LogisticRegression etc.) p/ ONNX
skl2onnx>=1.17.0
# onnx (utilitarios de validacao da estrutura ONNX apos export, versao >=1.16=wheel py313)
onnx>=1.16.0,<1.19.0

# =============================================================================
# OPCIONAL: Apache Airflow (recomendado usar via docker-compose-airflow.yml)
# Descomente abaixo apenas se deseja instalar localmente (pode gerar conflitos
# de dependencias; prefira a imagem oficial apache/airflow via compose).
# =============================================================================
# apache-airflow>=2.10.2
# apache-airflow-providers-postgres>=5.0.0
# psycopg2-binary>=2.9.0
